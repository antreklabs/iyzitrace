services:
  grafana:
    user: root
    container_name: 'antreklabs-iyzitrace-app'

    build:
      context: .
      args:
        grafana_image: grafana
        grafana_version: 12.2.0
        development: ${DEVELOPMENT:-false}
        anonymous_auth_enabled: ${ANONYMOUS_AUTH_ENABLED:-true}
    ports:
      - 3000:3000/tcp
    volumes:
      - ../grafana-data:/var/lib/grafana
      - ../dist:/var/lib/grafana/plugins/antreklabs-iyzitrace-app
      - ../provisioning:/etc/grafana/provisioning
      - ..:/root/antreklabs-iyzitrace-app
      - grafana-data:/var/lib/grafana

    environment:
      NODE_ENV: development
      GF_LOG_FILTERS: plugin.antreklabs-iyzitrace-app:debug
      GF_LOG_LEVEL: debug
      GF_DATAPROXY_LOGGING: 1
      GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS: antreklabs-iyzitrace-app
      GF_AUTH_ANONYMOUS_ENABLED: true
      GF_AUTH_ANONYMOUS_ORG_ROLE: Admin
      GF_AUTH_DISABLE_LOGIN_FORM: true
      GF_FEATURE_TOGGLES_ENABLE: traceqlEditor

volumes:
  grafana-data:
    driver: local