name: observability-platform

services:
  ########################
  # Backing data stores  #
  ########################

  tempo:
    container_name: tempo-platform
    image: grafana/tempo:latest
    command: [ "-config.file=/etc/tempo/tempo.yaml" ]
    volumes:
      - ./tempo/tempo.yaml:/etc/tempo/tempo.yaml:ro
      - tempo-data:/tmp/tempo
    ports:
      - "3201:3200" # Tempo HTTP (changed from 3200 to avoid conflict)
      - "14269:14268" # Jaeger ingest (changed from 14268 to avoid conflict)
      - "4319"   # OTLP gRPC (changed from 4317 to avoid conflict)
      - "4320"   # OTLP HTTP (changed from 4318 to avoid conflict)
    networks: [ otel ]

  loki:
    container_name: loki-platform
    image: grafana/loki:latest
    command: [ "-config.file=/etc/loki/config.yml" ]
    volumes:
      - ./loki/config.yml:/etc/loki/config.yml:ro
      - loki-data:/loki
    ports:
      - "3101:3100" # Changed from 3100 to avoid conflict
    networks: [ otel ]

  prometheus:
    container_name: prometheus-platform
    image: prom/prometheus:latest
    command:
      - --config.file=/etc/prometheus.yaml
      - --web.enable-remote-write-receiver
      - --enable-feature=exemplar-storage
      - --enable-feature=native-histograms
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus.yaml
    ports:
      - "9091:9090" # Changed from 9090 to avoid conflict
    networks: [ otel ]

  ########################
  # Server collectors    #
  ########################

  server_a: # traces -> Tempo
    container_name: server_a
    image: otel/opentelemetry-collector-contrib:latest
    command: [ "--config=/etc/otelcol/config.yaml" ]
    volumes:
      - ./otel/server_a.yaml:/etc/otelcol/config.yaml:ro
    ports:
      - "14317:4317" # optional, for debugging
      - "14318:4318"
    networks: [ otel ]
    depends_on: [ tempo ]

  server_b: # metrics -> Prometheus (prometheus exporter, scraped by Prometheus)
    container_name: server_b
    image: otel/opentelemetry-collector-contrib:latest
    command: [ "--config=/etc/otelcol/config.yaml" ]
    volumes:
      - ./otel/server_b.yaml:/etc/otelcol/config.yaml:ro
    ports:
      - "18889:8889" # expose server_b's /metrics for quick manual checks
      - "24317:4317"
      - "24318:4318"
    networks: [ otel ]
    depends_on: [ prometheus ]

  server_c: # logs -> Loki
    container_name: server_c
    image: otel/opentelemetry-collector-contrib:latest
    command: [ "--config=/etc/otelcol/config.yaml" ]
    volumes:
      - ./otel/server_c.yaml:/etc/otelcol/config.yaml:ro
    ports:
      - "34317:4317"
      - "34318:4318"
    networks: [ otel ]
    depends_on: [ loki ]

  ########################
  # NGINX signal router  #
  ########################

  nginx:
    container_name: nginx-platform
    image: nginx:1.27-alpine
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "4319:4317" # gRPC ingress (changed from 4317 to avoid conflict)
      - "4320:4318" # HTTP ingress (changed from 4318 to avoid conflict)
    networks: [ otel ]
    depends_on: [ server_a, server_b, server_c ]

  ########################
  # Client collectors    #
  ########################

  client_a: # traces only -> NGINX
    container_name: client_a
    image: otel/opentelemetry-collector-contrib:latest
    command: [ "--config=/etc/otelcol/config.yaml" ]
    volumes:
      - ./otel/client_a.yaml:/etc/otelcol/config.yaml:ro
    ports:
      - "5317:4317" # to receive from telemetrygen_traces
    networks: [ otel ]
    depends_on: [ nginx ]

  client_b: # metrics only -> NGINX
    container_name: client_b
    image: otel/opentelemetry-collector-contrib:latest
    command: [ "--config=/etc/otelcol/config.yaml" ]
    volumes:
      - ./otel/client_b.yaml:/etc/otelcol/config.yaml:ro
    ports:
      - "6317:4317" # to receive from telemetrygen_metrics
    networks: [ otel ]
    depends_on: [ nginx ]

  client_c: # logs only -> NGINX
    container_name: client_c
    image: otel/opentelemetry-collector-contrib:latest
    command: [ "--config=/etc/otelcol/config.yaml" ]
    volumes:
      - ./otel/client_c.yaml:/etc/otelcol/config.yaml:ro
    ports:
      - "7317:4317" # to receive from telemetrygen_logs
    networks: [ otel ]
    depends_on: [ nginx ]

  ########################
  # Telemetry generators #
  ########################

  telemetrygen_traces:
    container_name: telemetrygen_traces
    image: ghcr.io/open-telemetry/opentelemetry-collector-contrib/telemetrygen:latest
    command: ["traces", "--otlp-endpoint=client_a:4317", "--otlp-insecure", "--duration=inf", "--service=demo-traces"]
    networks: [ otel ]
    depends_on: [ client_a ]

  telemetrygen_metrics:
    container_name: telemetrygen_metrics
    image: ghcr.io/open-telemetry/opentelemetry-collector-contrib/telemetrygen:latest
    command: ["metrics", "--otlp-endpoint=client_b:4317", "--otlp-insecure", "--duration=inf", "--service=demo-metrics"]
    networks: [ otel ]
    depends_on: [ client_b ]

  telemetrygen_logs:
    container_name: telemetrygen_logs
    image: ghcr.io/open-telemetry/opentelemetry-collector-contrib/telemetrygen:latest
    command: ["logs", "--otlp-endpoint=client_c:4317", "--otlp-insecure", "--duration=inf", "--service=demo-logs"]
    networks: [ otel ]
    depends_on: [ client_c ]

networks:
  otel: {}

volumes:
  tempo-data: {}
  loki-data: {}
  prom-data: {}
