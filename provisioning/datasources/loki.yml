apiVersion: 1

datasources:
  - name: Loki (Observability Platform)
    type: loki
    uid: loki-platform
    access: proxy
    orgId: 1
    url: http://host.docker.internal/datasource/logs
    isDefault: false
    editable: true