apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: false
    uid: prometheus
    orgId: 1
    basicAuth: false
    version: 1
    editable: false
    jsonData:
      httpMethod: GET