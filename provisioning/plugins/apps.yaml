apiVersion: 1

apps:
  - type: 'easytrace-test-app'
    org_id: 1
    org_name: 'easytrace'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key
