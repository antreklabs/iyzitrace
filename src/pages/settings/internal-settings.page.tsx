import React from 'react';
import ConfigForm from '../../components/settings/config-form.component';

/**
 * Internal settings page for /a/antreklabs-iyzitrace-app/settings route
 * This is the settings page accessed from within the app
 */
const InternalSettingsPage: React.FC = () => {
    return <ConfigForm />;
};

export default InternalSettingsPage;
